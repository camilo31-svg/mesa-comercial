import records from "../../../lib/documents-data.json";import meta from "../../../lib/documents-meta.json";import {getChatGPTUser} from "../../chatgpt-auth";export const dynamic="force-dynamic";const norm=(s:string)=>s.normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase();const index=records.map(r=>({record:r,text:norm(r.source+" "+r.text)}));export async function GET(req:Request){if(!await getChatGPTUser())return Response.json({error:"Inicia sesión"},{status:401});const q=new URL(req.url).searchParams.get("q")||"";if(q.length>200)return Response.json({error:"Consulta demasiado larga"},{status:400});const words=norm(q).trim().split(/\s+/).filter(Boolean);const results=words.length?index.filter(r=>words.every(w=>r.text.includes(w))).slice(0,15).map(r=>r.record):[];return Response.json({results,meta},{headers:{"Cache-Control":"no-store"}});}